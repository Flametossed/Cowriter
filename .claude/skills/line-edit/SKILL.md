---
name: line-edit
description: Sentence-level polish on a structurally-sound draft — filler/filter words, redundancy, weak verbs, clunky constructions, ambiguous pronouns — while preserving the voice fingerprint. Use after macro passes (critique/pacing). Suggests by default, applies only if asked.
---

# line-edit

1. Confirm the passage is structurally settled (run `/critique` first if not).
2. Follow the **line-edit** section of [revision-methods.md](../../../frameworks/revision/revision-methods.md).
3. Preserve voice — never flatten deliberate fragments/rhythm/register (fingerprint wins).
4. Default to a marked `original → suggested + why` list; apply inline only if asked.
