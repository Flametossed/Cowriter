# Outline — {{PROJECT_TITLE}}

> Argument structure. Each section: one-line job + the fact IDs it uses + a word budget. Every section must move the thesis forward — a section with no job is a cut.

<!-- Structure default by goal (swap freely):
persuade → problem / stakes / solution / objections / call
inform   → inverted pyramid or chronological
instruct → prerequisites / steps / pitfalls
convert  → hook / pain / promise / proof / ask

Format: S<id>. Title — job — uses F<ids> — ~<n> words
Example:
S2. The hidden cost — make the reader feel the problem — uses F1, F3 — ~300 words
-->

- 
